{
  "name": "express-respond-simple",
  "type": "module",
  "version": "1.2.4",
  "description": "Creates simple express middleware for responding any status and body.",
  "main": "index.mjs",
  "module": "index.mjs",
  "types": "index.d.ts",
  "scripts": {
    "prepare": "run-s build",
    "build": "run-p build:*",
    "build:code": "(cat template.mjs && echo && node generate-exports.mjs) > index.mjs",
    "build:types": "(cat template.d.ts && echo && node generate-types.mjs) > index.d.ts",
    "test": "run-s lint mocha",
    "lint": "standard --fix",
    "mocha": "mocha"
  },
  "repository": "hugojosefson/express-respond-simple",
  "keywords": [
    "express",
    "respond",
    "simple",
    "response",
    "middleware",
    "http",
    "status",
    "statuscode"
  ],
  "author": "Hugo Josefson <hugo@josefson.org>",
  "license": "MIT",
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "@types/express": "^4.17.7",
    "@types/statuses": "^2.0.0",
    "camelcase": "^6.0.0",
    "chai": "^4.2.0",
    "chai-spies": "^1.0.0",
    "mocha": "^8.0.1",
    "npm-run-all": "^4.1.5",
    "standard": "^14.3.4"
  },
  "dependencies": {
    "statuses": "^2.0.0"
  }
}
